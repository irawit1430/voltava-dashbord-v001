🧹 Refactor explicit `any` to `ExternalDevicePayload` interface in `addOrUpdateExternalDevice`

🎯 **What:** Replaced the explicit `any` type for the `payload` parameter in `addOrUpdateExternalDevice` with a dedicated `ExternalDevicePayload` interface in `server/types.ts`.
💡 **Why:** Using an explicit interface improves code maintainability by providing strict typing for the device payload. This prevents errors when accessing properties and makes the code self-documenting.
✅ **Verification:** Verified by running the existing unit test suite (`npm run test`), lint checks (`npm run lint`), and TypeScript compiler (`npx tsc --noEmit`). All checks passed successfully. No new functionality was introduced, avoiding backwards-incompatible changes by making all properties optional.
✨ **Result:** Enhanced readability, improved maintainability, and introduced compile-time type safety within the simulator module.
